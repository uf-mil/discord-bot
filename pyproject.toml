[project]
authors = [
  {name = "Cameron Brown", email = "me@cbrxyz.com"},
]
version = "1.0.0"
name = "discord-bot"

[tool.setuptools]
packages = ["src", "src.github", "src.reports", "src.assets"]

[project.scripts]
discord-bot = "src.bot:main"
discord-bot-webhook-server = "src.webhook_server:main"
